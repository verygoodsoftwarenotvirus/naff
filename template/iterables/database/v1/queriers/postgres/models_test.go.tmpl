package postgres

import (
	"context"
	"database/sql"
	"errors"
	"testing"
	"time"

	"github.com/DATA-DOG/go-sqlmock"

	"{{ .OutputRepository }}/models/v1"

	"github.com/stretchr/testify/assert"
)
func buildMockRowFrom{{ pascal .Name }}({{ camelCase .Name}} *models.{{ pascal .Name }}) *sqlmock.Rows {
	exampleRows := sqlmock.NewRows({{ camelCase .Name}}sTableColumns).
		AddRow(
			{{ camelCase .Name}}.ID,
		{{ $og := . }}{{ range $j, $field := .Fields }}	
			{{ camelCase $og.Name}}.{{ pascal $field.Name }},
		{{ end }}	
			{{ camelCase .Name}}.CreatedOn,
			{{ camelCase .Name}}.UpdatedOn,
			{{ camelCase .Name}}.ArchivedOn,
			{{ camelCase .Name}}.BelongsTo,
		)

	return exampleRows
}

func buildErroneousMockRowFrom{{ pascal .Name }}({{ camelCase .Name}} *models.{{ pascal .Name }}) *sqlmock.Rows {
	exampleRows := sqlmock.NewRows({{ camelCase .Name}}sTableColumns).
		AddRow(
			{{ camelCase .Name}}.ArchivedOn,
		{{ $og := . }}{{ range $j, $field := .Fields }}	
			{{ camelCase $og.Name}}.{{ pascal $field.Name }},
		{{ end }}	
			{{ camelCase .Name}}.CreatedOn,
			{{ camelCase .Name}}.UpdatedOn,
			{{ camelCase .Name}}.BelongsTo,
			{{ camelCase .Name}}.ID,
		)

	return exampleRows
}

func TestPostgres_buildGet{{ camelcase .Name }}Query(T *testing.T) {
	T.Parallel()

	T.Run("happy path", func(t *testing.T) {
		p, _ := buildTestService(t)
		example{{ camelcase .Name }}ID := uint64(123)
		exampleUserID := uint64(321)

		expectedArgCount := 2
		expectedQuery := "SELECT id, created_on, updated_on, archived_on, belongs_to FROM {{ lower .Name }}s WHERE belongs_to = $1 AND id = $2"

		actualQuery, args := p.buildGet{{ camelcase .Name }}Query(example{{ camelcase .Name }}ID, exampleUserID)
		assert.Equal(t, expectedQuery, actualQuery)
		assert.Len(t, args, expectedArgCount)
		assert.Equal(t, exampleUserID, args[0].(uint64))
		assert.Equal(t, example{{ camelcase .Name }}ID, args[1].(uint64))
	})
}

func TestPostgres_Get{{ camelcase .Name }}(T *testing.T) {
	T.Parallel()

	T.Run("happy path", func(t *testing.T) {
		expectedQuery := "SELECT id, created_on, updated_on, archived_on, belongs_to FROM {{ lower .Name }}s WHERE belongs_to = $1 AND id = $2"
		expected := &models.{{ camelcase .Name }}{
			ID:      123,
			
		{{ range $j, $field := .Fields }}	
			{{ pascal $field.Name }}: {{ typeExample $field.Type $field.Pointer }},
		{{ end }}
		}
		expectedUserID := uint64(321)

		p, mockDB := buildTestService(t)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedQuery)).
			WithArgs(expectedUserID, expected.ID).
			WillReturnRows(
				buildMockRowFrom{{ camelcase .Name }}(expected),
			)

		actual, err := p.Get{{ camelcase .Name }}(context.Background(), expected.ID, expectedUserID)
		assert.NoError(t, err)
		assert.Equal(t, expected, actual)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})

	T.Run("surfaces sql.ErrNoRows", func(t *testing.T) {
		expectedQuery := "SELECT id, created_on, updated_on, archived_on, belongs_to FROM {{ lower .Name }}s WHERE belongs_to = $1 AND id = $2"
		expected := &models.{{ camelcase .Name }}{
			ID:      123,
			
		{{ range $j, $field := .Fields }}	
			{{ pascal $field.Name }}: {{ typeExample $field.Type $field.Pointer }},
		{{ end }}
		}
		expectedUserID := uint64(321)

		p, mockDB := buildTestService(t)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedQuery)).
			WithArgs(expectedUserID, expected.ID).
			WillReturnError(sql.ErrNoRows)

		actual, err := p.Get{{ camelcase .Name }}(context.Background(), expected.ID, expectedUserID)
		assert.Error(t, err)
		assert.Nil(t, actual)
		assert.Equal(t, sql.ErrNoRows, err)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})
}

func TestPostgres_buildGet{{ camelcase .Name }}CountQuery(T *testing.T) {
	T.Parallel()

	T.Run("happy path", func(t *testing.T) {
		p, _ := buildTestService(t)
		exampleUserID := uint64(321)

		expectedArgCount := 1
		expectedQuery := "SELECT COUNT(id) FROM {{ lower .Name }}s WHERE archived_on IS NULL AND belongs_to = $1 LIMIT 20"

		actualQuery, args := p.buildGet{{ camelcase .Name }}CountQuery(models.DefaultQueryFilter(), exampleUserID)
		assert.Equal(t, expectedQuery, actualQuery)
		assert.Len(t, args, expectedArgCount)
		assert.Equal(t, exampleUserID, args[0].(uint64))
	})
}

func TestPostgres_Get{{ camelcase .Name }}Count(T *testing.T) {
	T.Parallel()

	T.Run("happy path", func(t *testing.T) {
		expectedUserID := uint64(321)
		expectedQuery := "SELECT COUNT(id) FROM {{ lower .Name }}s WHERE archived_on IS NULL AND belongs_to = $1 LIMIT 20"
		expectedCount := uint64(666)

		p, mockDB := buildTestService(t)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedQuery)).
			WithArgs(expectedUserID).
			WillReturnRows(
				sqlmock.NewRows([]string{"count"}).AddRow(expectedCount),
			)

		actualCount, err := p.Get{{ camelcase .Name }}Count(context.Background(), models.DefaultQueryFilter(), expectedUserID)
		assert.NoError(t, err)
		assert.Equal(t, expectedCount, actualCount)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})
}

func TestPostgres_buildGetAll{{ camelcase .Name }}sCountQuery(T *testing.T) {
	T.Parallel()

	T.Run("happy path", func(t *testing.T) {
		p, _ := buildTestService(t)
		expectedQuery := "SELECT COUNT(id) FROM {{ lower .Name }}s WHERE archived_on IS NULL"

		actualQuery := p.buildGetAll{{ camelcase .Name }}sCountQuery()
		assert.Equal(t, expectedQuery, actualQuery)
	})
}

func TestPostgres_GetAll{{ camelcase .Name }}sCount(T *testing.T) {
	T.Parallel()

	T.Run("happy path", func(t *testing.T) {
		expectedQuery := "SELECT COUNT(id) FROM {{ lower .Name }}s WHERE archived_on IS NULL"
		expectedCount := uint64(666)

		p, mockDB := buildTestService(t)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedQuery)).
			WillReturnRows(
				sqlmock.NewRows([]string{"count"}).AddRow(expectedCount),
			)

		actualCount, err := p.GetAll{{ camelcase .Name }}sCount(context.Background())
		assert.NoError(t, err)
		assert.Equal(t, expectedCount, actualCount)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})
}

func TestPostgres_buildGet{{ camelcase .Name }}sQuery(T *testing.T) {
	T.Parallel()

	T.Run("happy path", func(t *testing.T) {
		p, _ := buildTestService(t)
		exampleUserID := uint64(321)

		expectedArgCount := 1
		expectedQuery := "SELECT id, created_on, updated_on, archived_on, belongs_to FROM {{ lower .Name }}s WHERE archived_on IS NULL AND belongs_to = $1 LIMIT 20"

		actualQuery, args := p.buildGet{{ camelcase .Name }}sQuery(models.DefaultQueryFilter(), exampleUserID)
		assert.Equal(t, expectedQuery, actualQuery)
		assert.Len(t, args, expectedArgCount)
		assert.Equal(t, exampleUserID, args[0].(uint64))
	})
}

func TestPostgres_Get{{ camelcase .Name }}s(T *testing.T) {
	T.Parallel()

	T.Run("happy path", func(t *testing.T) {
		expectedUserID := uint64(123)
		expected{{ camelcase .Name }}1 := &models.{{ camelcase .Name }}{
			
		{{ range $j, $field := .Fields }}	
			{{ pascal $field.Name }}: {{ typeExample $field.Type $field.Pointer }},
		{{ end }}	
			
		}

		expectedListQuery := "SELECT id, created_on, updated_on, archived_on, belongs_to FROM {{ lower .Name }}s WHERE archived_on IS NULL AND belongs_to = $1 LIMIT 20"
		expectedCountQuery := "SELECT COUNT(id) FROM {{ lower .Name }}s WHERE archived_on IS NULL"
		expectedCount := uint64(666)

		p, mockDB := buildTestService(t)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedListQuery)).
			WithArgs(expectedUserID).
			WillReturnRows(
				buildMockRowFrom{{ camelcase .Name }}(expected{{ camelcase .Name }}1),
			)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedCountQuery)).
			WillReturnRows(
				sqlmock.NewRows([]string{"count"}).AddRow(expectedCount),
			)

		expected := &models.{{ camelcase .Name }}List{
			Pagination: models.Pagination{
				Page:       1,
				Limit:      20,
				TotalCount: expectedCount,
			},
			{{ camelcase .Name }}s: []models.{{ camelcase .Name }}{
				*expected{{ camelcase .Name }}1,
			},
		}

		actual, err := p.Get{{ camelcase .Name }}s(context.Background(), models.DefaultQueryFilter(), expectedUserID)
		assert.NoError(t, err)
		assert.Equal(t, expected, actual)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})

	T.Run("surfaces sql.ErrNoRows", func(t *testing.T) {
		expectedUserID := uint64(123)
		expectedListQuery := "SELECT id, created_on, updated_on, archived_on, belongs_to FROM {{ lower .Name }}s WHERE archived_on IS NULL AND belongs_to = $1 LIMIT 20"

		p, mockDB := buildTestService(t)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedListQuery)).
			WithArgs(expectedUserID).
			WillReturnError(sql.ErrNoRows)

		actual, err := p.Get{{ camelcase .Name }}s(context.Background(), models.DefaultQueryFilter(), expectedUserID)
		assert.Error(t, err)
		assert.Nil(t, actual)
		assert.Equal(t, sql.ErrNoRows, err)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})

	T.Run("with error executing read query", func(t *testing.T) {
		expectedUserID := uint64(123)

		expectedListQuery := "SELECT id, created_on, updated_on, archived_on, belongs_to FROM {{ lower .Name }}s WHERE archived_on IS NULL AND belongs_to = $1 LIMIT 20"
		p, mockDB := buildTestService(t)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedListQuery)).
			WithArgs(expectedUserID).
			WillReturnError(errors.New("blah"))

		actual, err := p.Get{{ camelcase .Name }}s(context.Background(), models.DefaultQueryFilter(), expectedUserID)
		assert.Error(t, err)
		assert.Nil(t, actual)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})

	T.Run("with error scanning {{ lower .Name }}", func(t *testing.T) {
		expectedUserID := uint64(123)
		expected{{ camelcase .Name }}1 := &models.{{ camelcase .Name }}{
			{{ range $j, $field := .Fields }}	
			{{ pascal $field.Name }}: {{ typeExample $field.Type $field.Pointer }},
			{{ end }}	
		}

		expectedListQuery := "SELECT id, created_on, updated_on, archived_on, belongs_to FROM {{ lower .Name }}s WHERE archived_on IS NULL AND belongs_to = $1 LIMIT 20"

		p, mockDB := buildTestService(t)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedListQuery)).
			WithArgs(expectedUserID).
			WillReturnRows(
				buildErroneousMockRowFrom{{ camelcase .Name }}(expected{{ camelcase .Name }}1),
			)

		actual, err := p.Get{{ camelcase .Name }}s(context.Background(), models.DefaultQueryFilter(), expectedUserID)
		assert.Error(t, err)
		assert.Nil(t, actual)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})

	T.Run("with error querying for count", func(t *testing.T) {
		expectedUserID := uint64(123)
		expected{{ camelcase .Name }}1 := &models.{{ camelcase .Name }}{
			{{ range $j, $field := .Fields }}	
			{{ pascal $field.Name }}: {{ typeExample $field.Type $field.Pointer }},
			{{ end }}	
		}

		expectedListQuery := "SELECT id, created_on, updated_on, archived_on, belongs_to FROM {{ lower .Name }}s WHERE archived_on IS NULL AND belongs_to = $1 LIMIT 20"
		expectedCountQuery := "SELECT COUNT(id) FROM {{ lower .Name }}s WHERE archived_on IS NULL"

		p, mockDB := buildTestService(t)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedListQuery)).
			WithArgs(expectedUserID).
			WillReturnRows(
				buildMockRowFrom{{ camelcase .Name }}(expected{{ camelcase .Name }}1),
			)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedCountQuery)).
			WillReturnError(errors.New("blah"))

		actual, err := p.Get{{ camelcase .Name }}s(context.Background(), models.DefaultQueryFilter(), expectedUserID)
		assert.Error(t, err)
		assert.Nil(t, actual)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})

}

func TestPostgres_GetAll{{ camelcase .Name }}sForUser(T *testing.T) {
	T.Parallel()

	T.Run("happy path", func(t *testing.T) {
		expectedUserID := uint64(123)
		expected{{ camelcase .Name }} := &models.{{ camelcase .Name }}{
			{{ range $j, $field := .Fields }}	
			{{ pascal $field.Name }}: {{ typeExample $field.Type $field.Pointer }},
			{{ end }}	
		}
		expectedListQuery := "SELECT id, created_on, updated_on, archived_on, belongs_to FROM {{ lower .Name }}s WHERE archived_on IS NULL AND belongs_to = $1"

		p, mockDB := buildTestService(t)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedListQuery)).
			WithArgs(expectedUserID).
			WillReturnRows(
				buildMockRowFrom{{ camelcase .Name }}(expected{{ camelcase .Name }}),
			)

		expected := []models.{{ camelcase .Name }}{*expected{{ camelcase .Name }}}

		actual, err := p.GetAll{{ camelcase .Name }}sForUser(context.Background(), expectedUserID)
		assert.NoError(t, err)
		assert.Equal(t, expected, actual)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})

	T.Run("surfaces sql.ErrNoRows", func(t *testing.T) {
		expectedUserID := uint64(123)
		expectedListQuery := "SELECT id, created_on, updated_on, archived_on, belongs_to FROM {{ lower .Name }}s WHERE archived_on IS NULL AND belongs_to = $1"

		p, mockDB := buildTestService(t)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedListQuery)).
			WithArgs(expectedUserID).
			WillReturnError(sql.ErrNoRows)

		actual, err := p.GetAll{{ camelcase .Name }}sForUser(context.Background(), expectedUserID)
		assert.Error(t, err)
		assert.Nil(t, actual)
		assert.Equal(t, sql.ErrNoRows, err)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})

	T.Run("with error querying database", func(t *testing.T) {
		expectedUserID := uint64(123)
		expectedListQuery := "SELECT id, created_on, updated_on, archived_on, belongs_to FROM {{ lower .Name }}s WHERE archived_on IS NULL AND belongs_to = $1"

		p, mockDB := buildTestService(t)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedListQuery)).
			WithArgs(expectedUserID).
			WillReturnError(errors.New("blah"))

		actual, err := p.GetAll{{ camelcase .Name }}sForUser(context.Background(), expectedUserID)
		assert.Error(t, err)
		assert.Nil(t, actual)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})

	T.Run("with unscannable response", func(t *testing.T) {
		expectedUserID := uint64(123)
		expected{{ camelcase .Name }} := &models.{{ camelcase .Name }}{
			{{ range $j, $field := .Fields }}	
			{{ pascal $field.Name }}: {{ typeExample $field.Type $field.Pointer }},
			{{ end }}	
		}
		expectedListQuery := "SELECT id, created_on, updated_on, archived_on, belongs_to FROM {{ lower .Name }}s WHERE archived_on IS NULL AND belongs_to = $1"

		p, mockDB := buildTestService(t)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedListQuery)).
			WithArgs(expectedUserID).
			WillReturnRows(
				buildErroneousMockRowFrom{{ camelcase .Name }}(expected{{ camelcase .Name }}),
			)

		actual, err := p.GetAll{{ camelcase .Name }}sForUser(context.Background(), expectedUserID)
		assert.Error(t, err)
		assert.Nil(t, actual)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})
}

func TestPostgres_buildCreate{{ camelcase .Name }}Query(T *testing.T) {
	T.Parallel()

	T.Run("happy path", func(t *testing.T) {
		p, _ := buildTestService(t)
		expected := &models.{{ camelcase .Name }}{
			
		{{ range $j, $field := .Fields }}	
			{{ pascal $field.Name }}: {{ typeExample $field.Type $field.Pointer }},
		{{ end }}
			BelongsTo: 123,
		}

		expectedArgCount := 3
		expectedQuery := "INSERT INTO {{ lower .Name }}s (belongs_to) VALUES ($1,$2,$3) RETURNING id, created_on"

		actualQuery, args := p.buildCreate{{ camelcase .Name }}Query(expected)
		assert.Equal(t, expectedQuery, actualQuery)
		assert.Len(t, args, expectedArgCount)
		{{ range $j, $field := .Fields }}	
		assert.Equal(t, expected.{{ pascal $field.Name }}, args[{{ $j }}].({{ $field.Type }}))
		{{ end }}	
		assert.Equal(t, expected.BelongsTo, args[len(args)-1].(uint64))
	})
}

func TestPostgres_Create{{ pascal .Name }}(T *testing.T) {
	T.Parallel()
	
	T.Run("happy path", func(t *testing.T) {
		expectedUserID := uint64(321)
		expected := &models.{{ pascal .Name }}{
			ID:        123,
			BelongsTo: expectedUserID,
			CreatedOn: uint64(time.Now().Unix()),
		}
		expectedInput := &models.{{ pascal .Name }}CreationInput{
			{{ range $j, $field := .Fields }}
				{{ if $field.ValidForCreationInput }}
					{{ pascal $field.Name }}: expected.{{ pascal $field.Name }},
				{{ end }}
			{{ end }}	
			BelongsTo: expected.BelongsTo,
		}
		exampleRows := sqlmock.NewRows([]string{"id", "created_on"}).
			AddRow(expected.ID, uint64(time.Now().Unix()))

		expectedQuery := "INSERT INTO {{ snakecase .Name }} (belongs_to) VALUES ($1,$2,$3) RETURNING id, created_on"

		p, mockDB := buildTestService(t)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedQuery)).
			WithArgs(
			{{ $og := . }}{{ range $j, $field := .Fields }}	
				expected.{{ pascal $field.Name }},
			{{ end }}	
				expected.BelongsTo,
			).
			WillReturnRows(exampleRows)

		actual, err := p.Create{{ pascal .Name }}(context.Background(), expectedInput)
		assert.NoError(t, err)
		assert.Equal(t, expected, actual)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})

	T.Run("with error writing to database", func(t *testing.T) {
		expectedUserID := uint64(321)
		example := &models.{{ pascal .Name }}{
			ID:        123,
			BelongsTo: expectedUserID,
			CreatedOn: uint64(time.Now().Unix()),
		}
		expectedInput := &models.{{ pascal .Name }}CreationInput{
			BelongsTo: example.BelongsTo,
		}

		expectedQuery := "INSERT INTO {{ snakecase .Name }} (belongs_to) VALUES ($1,$2,$3) RETURNING id, created_on"

		p, mockDB := buildTestService(t)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedQuery)).
			WithArgs(
			{{ $og := . }}{{ range $j, $field := .Fields }}	
				example.{{ pascal $field.Name }},
			{{ end }}	
				example.BelongsTo,
			).
			WillReturnError(errors.New("blah"))

		actual, err := p.Create{{ pascal .Name }} (context.Background(), expectedInput)
		assert.Error(t, err)
		assert.Nil(t, actual)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})
}
func TestPostgres_buildUpdate{{ camelcase .Name }}Query(T *testing.T) {
	T.Parallel()

	T.Run("happy path", func(t *testing.T) {
		p, _ := buildTestService(t)
		expected := &models.{{ camelcase .Name }}{
			ID:        321,
			
		{{ range $j, $field := .Fields }}	
			{{ pascal $field.Name }}: {{ typeExample $field.Type $field.Pointer }},
		{{ end }}
			BelongsTo: 123,
		}

		expectedArgCount := 4
		expectedQuery := "UPDATE {{ lower .Name }}s SET updated_on = extract(epoch FROM NOW()) WHERE belongs_to = $3 AND id = $4 RETURNING updated_on"

		actualQuery, args := p.buildUpdate{{ camelcase .Name }}Query(expected)
		assert.Equal(t, expectedQuery, actualQuery)
		assert.Len(t, args, expectedArgCount)
		{{ range $j, $field := .Fields }}	
		assert.Equal(t, expected.{{ pascal $field.Name }}, args[{{ $j }}].({{ $field.Type }}))
		{{ end }}	
		assert.Equal(t, expected.BelongsTo, args[len(args)-1].(uint64))
		assert.Equal(t, expected.ID, args[3].(uint64))
	})
}

func TestPostgres_Update{{ camelcase .Name }}(T *testing.T) {
	T.Parallel()

	T.Run("happy path", func(t *testing.T) {
		expectedUserID := uint64(321)
		expected := &models.{{ camelcase .Name }}{
			ID:        123,
			{{ range $j, $field := .Fields }}	
			{{ pascal $field.Name }}: {{ typeExample $field.Type $field.Pointer }},
			{{ end }}	
			BelongsTo: expectedUserID,
			CreatedOn: uint64(time.Now().Unix()),
		}
		exampleRows := sqlmock.NewRows([]string{"updated_on"}).
			AddRow(uint64(time.Now().Unix()))

		expectedQuery := "UPDATE {{ lower .Name }}s SET updated_on = extract(epoch FROM NOW()) WHERE belongs_to = $3 AND id = $4 RETURNING updated_on"

		p, mockDB := buildTestService(t)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedQuery)).
			WithArgs(
				{{ range $j, $field := .Fields }}	
					expected.{{ pascal $field.Name }},
				{{ end }}	
				expected.BelongsTo,
				expected.ID,
			).
			WillReturnRows(exampleRows)

		err := p.Update{{ camelcase .Name }}(context.Background(), expected)
		assert.NoError(t, err)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})

	T.Run("with error writing to database", func(t *testing.T) {
		expectedUserID := uint64(321)
		example := &models.{{ camelcase .Name }}{
			ID:        123,
			{{ range $j, $field := .Fields }}	
			{{ pascal $field.Name }}: {{ typeExample $field.Type $field.Pointer }},
			{{ end }}	
			BelongsTo: expectedUserID,
			CreatedOn: uint64(time.Now().Unix()),
		}

		expectedQuery := "UPDATE {{ lower .Name }}s SET updated_on = extract(epoch FROM NOW()) WHERE belongs_to = $3 AND id = $4 RETURNING updated_on"

		p, mockDB := buildTestService(t)
		mockDB.ExpectQuery(formatQueryForSQLMock(expectedQuery)).
			WithArgs(
			{{ range $j, $field := .Fields }}	
				example.{{ pascal $field.Name }},
			{{ end }}	
				example.BelongsTo,
				example.ID,
			).
			WillReturnError(errors.New("blah"))

		err := p.Update{{ camelcase .Name }}(context.Background(), example)
		assert.Error(t, err)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})
}

func TestPostgres_buildArchive{{ camelcase .Name }}Query(T *testing.T) {
	T.Parallel()

	T.Run("happy path", func(t *testing.T) {
		p, _ := buildTestService(t)
		expected := &models.{{ camelcase .Name }}{
			ID:        321,
			
		{{ range $j, $field := .Fields }}	
			{{ pascal $field.Name }}: {{ typeExample $field.Type $field.Pointer }},
		{{ end }}
			BelongsTo: 123,
		}

		expectedArgCount := 2
		expectedQuery := "UPDATE {{ lower .Name }}s SET updated_on = extract(epoch FROM NOW()), archived_on = extract(epoch FROM NOW()) WHERE archived_on IS NULL AND belongs_to = $1 AND id = $2 RETURNING archived_on"

		actualQuery, args := p.buildArchive{{ camelcase .Name }}Query(expected.ID, expected.BelongsTo)
		assert.Equal(t, expectedQuery, actualQuery)
		assert.Len(t, args, expectedArgCount)
		assert.Equal(t, expected.BelongsTo, args[0].(uint64))
		assert.Equal(t, expected.ID, args[1].(uint64))
	})
}

func TestPostgres_Archive{{ camelcase .Name }}(T *testing.T) {
	T.Parallel()

	T.Run("happy path", func(t *testing.T) {
		expectedUserID := uint64(321)
		expected := &models.{{ camelcase .Name }}{
			ID:        123,
			{{ range $j, $field := .Fields }}	
			{{ pascal $field.Name }}: {{ typeExample $field.Type $field.Pointer }},
			{{ end }}	
			BelongsTo: expectedUserID,
			CreatedOn: uint64(time.Now().Unix()),
		}

		expectedQuery := "UPDATE {{ lower .Name }}s SET updated_on = extract(epoch FROM NOW()), archived_on = extract(epoch FROM NOW()) WHERE archived_on IS NULL AND belongs_to = $1 AND id = $2 RETURNING archived_on"

		p, mockDB := buildTestService(t)
		mockDB.ExpectExec(formatQueryForSQLMock(expectedQuery)).
			WithArgs(
				expected.BelongsTo,
				expected.ID,
			).
			WillReturnResult(sqlmock.NewResult(1, 1))

		err := p.Archive{{ camelcase .Name }}(context.Background(), expected.ID, expectedUserID)
		assert.NoError(t, err)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})

	T.Run("with error writing to database", func(t *testing.T) {
		expectedUserID := uint64(321)
		example := &models.{{ camelcase .Name }}{
			ID:        123,
			{{ range $j, $field := .Fields }}	
			{{ pascal $field.Name }}: {{ typeExample $field.Type $field.Pointer }},
			{{ end }}	
			BelongsTo: expectedUserID,
			CreatedOn: uint64(time.Now().Unix()),
		}

		expectedQuery := "UPDATE {{ lower .Name }}s SET updated_on = extract(epoch FROM NOW()), archived_on = extract(epoch FROM NOW()) WHERE archived_on IS NULL AND belongs_to = $1 AND id = $2 RETURNING archived_on"

		p, mockDB := buildTestService(t)
		mockDB.ExpectExec(formatQueryForSQLMock(expectedQuery)).
			WithArgs(
				example.BelongsTo,
				example.ID,
			).
			WillReturnError(errors.New("blah"))

		err := p.Archive{{ camelcase .Name }}(context.Background(), example.ID, expectedUserID)
		assert.Error(t, err)

		assert.NoError(t, mockDB.ExpectationsWereMet(), "not all database expectations were met")
	})
}
