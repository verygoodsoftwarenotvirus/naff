package {{ lower .Name }}s

import (
	"database/sql"
	"net/http"
	"strconv"

	"{{ .OutputRepository }}/models/v1"

	"gitlab.com/verygoodsoftwarenotvirus/newsman"

	"go.opencensus.io/trace"
)

const (
	// URIParamKey is a standard string that we'll use to refer to {{ lower .Name }} IDs with
	URIParamKey = "{{ lower .Name }}ID"
)

func attach{{ camelcase .Name }}IDToSpan(span *trace.Span, {{ lower .Name }}ID uint64) {
	if span != nil {
		span.AddAttributes(trace.StringAttribute("{{ lower .Name }}_id", strconv.FormatUint({{ lower .Name }}ID, 10)))
	}
}

func attachUserIDToSpan(span *trace.Span, userID uint64) {
	if span != nil {
		span.AddAttributes(trace.StringAttribute("user_id", strconv.FormatUint(userID, 10)))
	}
}

// ListHandler is our list route
func (s *Service) ListHandler(res http.ResponseWriter, req *http.Request) {
	ctx, span := trace.StartSpan(req.Context(), "ListHandler")
	defer span.End()

	// ensure query filter
	qf := models.ExtractQueryFilter(req)

	// determine user ID
	userID := s.userIDFetcher(req)
	logger := s.logger.WithValue("user_id", userID)

	attachUserIDToSpan(span, userID)

	// fetch {{ lower .Name }}s from database
	{{ lower .Name }}s, err := s.{{ lower .Name }}Database.Get{{ camelcase .Name }}s(ctx, qf, userID)
	if err == sql.ErrNoRows {
		// in the event no rows exist return an empty list
		{{ lower .Name }}s = &models.{{ camelcase .Name }}List{
			{{ camelcase .Name }}s: []models.{{ camelcase .Name }}{},
		}
	} else if err != nil {
		logger.Error(err, "error encountered fetching {{ lower .Name }}s")
		res.WriteHeader(http.StatusInternalServerError)
		return
	}

	// encode our response and peace
	if err = s.encoderDecoder.EncodeResponse(res, {{ lower .Name }}s); err != nil {
		s.logger.Error(err, "encoding response")
	}
}

// CreateHandler is our {{ lower .Name }} creation route
func (s *Service) CreateHandler(res http.ResponseWriter, req *http.Request) {
	ctx, span := trace.StartSpan(req.Context(), "CreateHandler")
	defer span.End()

	// determine user ID
	userID := s.userIDFetcher(req)
	attachUserIDToSpan(span, userID)
	logger := s.logger.WithValue("user_id", userID)

	// check request context for parsed input struct
	input, ok := ctx.Value(CreateMiddlewareCtxKey).(*models.{{ camelcase .Name }}CreationInput)
	logger = logger.WithValue("input", input)
	if !ok {
		logger.Info("valid input not attached to request")
		res.WriteHeader(http.StatusBadRequest)
		return
	}
	input.BelongsTo = userID

	// create {{ lower .Name }} in database
	x, err := s.{{ lower .Name }}Database.Create{{ camelcase .Name }}(ctx, input)
	if err != nil {
		s.logger.Error(err, "error creating {{ lower .Name }}")
		res.WriteHeader(http.StatusInternalServerError)
		return
	}

	// notify relevant parties of new {{ lower .Name }}
	s.{{ lower .Name }}Counter.Increment(ctx)
	attach{{ camelcase .Name }}IDToSpan(span, x.ID)
	s.reporter.Report(newsman.Event{
		Data:      x,
		Topics:    []string{topicName},
		EventType: string(models.Create),
	})

	// encode response and peace
	res.WriteHeader(http.StatusCreated)
	if err = s.encoderDecoder.EncodeResponse(res, x); err != nil {
		s.logger.Error(err, "encoding response")
	}
}

// ReadHandler returns a GET handler that returns an {{ lower .Name }}
func (s *Service) ReadHandler(res http.ResponseWriter, req *http.Request) {
	ctx, span := trace.StartSpan(req.Context(), "ReadHandler")
	defer span.End()

	// determine relevant info
	userID := s.userIDFetcher(req)
	{{ lower .Name }}ID := s.{{ lower .Name }}IDFetcher(req)

	// keep the aforementioned context in mind
	logger := s.logger.WithValues(map[string]interface{}{
		"user_id": userID,
		"{{ lower .Name }}_id": {{ lower .Name }}ID,
	})
	attach{{ camelcase .Name }}IDToSpan(span, {{ lower .Name }}ID)
	attachUserIDToSpan(span, userID)

	// fetch {{ lower .Name }} from database
	x, err := s.{{ lower .Name }}Database.Get{{ camelcase .Name }}(ctx, {{ lower .Name }}ID, userID)
	if err == sql.ErrNoRows {
		res.WriteHeader(http.StatusNotFound)
		return
	} else if err != nil {
		logger.Error(err, "error fetching {{ lower .Name }} from database")
		res.WriteHeader(http.StatusInternalServerError)
		return
	}

	// encode response and peace
	if err = s.encoderDecoder.EncodeResponse(res, x); err != nil {
		s.logger.Error(err, "encoding response")
	}
}

// UpdateHandler returns a handler that updates an {{ lower .Name }}
func (s *Service) UpdateHandler(res http.ResponseWriter, req *http.Request) {
	ctx, span := trace.StartSpan(req.Context(), "UpdateHandler")
	defer span.End()

	// check for parsed input attached to request context
	input, ok := ctx.Value(UpdateMiddlewareCtxKey).(*models.{{ camelcase .Name }}UpdateInput)
	if !ok {
		s.logger.Info("no input attached to request")
		res.WriteHeader(http.StatusBadRequest)
		return
	}

	// determine relevant info
	userID := s.userIDFetcher(req)
	{{ lower .Name }}ID := s.{{ lower .Name }}IDFetcher(req)

	// keep aforementioned context in mind
	logger := s.logger.WithValues(map[string]interface{}{
		"user_id": userID,
		"{{ lower .Name }}_id": {{ lower .Name }}ID,
	})
	attach{{ camelcase .Name }}IDToSpan(span, {{ lower .Name }}ID)
	attachUserIDToSpan(span, userID)

	// fetch {{ lower .Name }} from database
	x, err := s.{{ lower .Name }}Database.Get{{ camelcase .Name }}(ctx, {{ lower .Name }}ID, userID)
	if err == sql.ErrNoRows {
		res.WriteHeader(http.StatusNotFound)
		return
	} else if err != nil {
		logger.Error(err, "error encountered getting {{ lower .Name }}")
		res.WriteHeader(http.StatusInternalServerError)
		return
	}

	// update the data structure
	x.Update(input)

	// update {{ lower .Name }} in database
	if err = s.{{ lower .Name }}Database.Update{{ camelcase .Name }}(ctx, x); err != nil {
		logger.Error(err, "error encountered updating {{ lower .Name }}")
		res.WriteHeader(http.StatusInternalServerError)
		return
	}

	// notify relevant parties
	s.reporter.Report(newsman.Event{
		Data:      x,
		Topics:    []string{topicName},
		EventType: string(models.Update),
	})

	// encode response and peace
	if err = s.encoderDecoder.EncodeResponse(res, x); err != nil {
		s.logger.Error(err, "encoding response")
	}
}

// ArchiveHandler returns a handler that archives an {{ lower .Name }}
func (s *Service) ArchiveHandler(res http.ResponseWriter, req *http.Request) {
	ctx, span := trace.StartSpan(req.Context(), "ArchiveHandler")
	defer span.End()

	// determine relevant information
	userID := s.userIDFetcher(req)
	{{ lower .Name }}ID := s.{{ lower .Name }}IDFetcher(req)

	// keep aforementioned context in mind
	logger := s.logger.WithValues(map[string]interface{}{
		"{{ lower .Name }}_id": {{ lower .Name }}ID,
		"user_id": userID,
	})
	attach{{ camelcase .Name }}IDToSpan(span, {{ lower .Name }}ID)
	attachUserIDToSpan(span, userID)

	// archive the {{ lower .Name }} in the database
	err := s.{{ lower .Name }}Database.Archive{{ camelcase .Name }}(ctx, {{ lower .Name }}ID, userID)
	if err == sql.ErrNoRows {
		res.WriteHeader(http.StatusNotFound)
		return
	} else if err != nil {
		logger.Error(err, "error encountered deleting {{ lower .Name }}")
		res.WriteHeader(http.StatusInternalServerError)
		return
	}

	// notify relevant parties
	s.{{ lower .Name }}Counter.Decrement(ctx)
	s.reporter.Report(newsman.Event{
		EventType: string(models.Archive),
		Data:      &models.{{ camelcase .Name }}{ID: {{ lower .Name }}ID},
		Topics:    []string{topicName},
	})

	// peace
	res.WriteHeader(http.StatusNoContent)
}
