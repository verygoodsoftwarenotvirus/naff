package {{ lower .Name }}s

import (
	"{{ .OutputRepository }}/database/v1"
	"{{ .OutputRepository }}/models/v1"

	"github.com/google/wire"
)

var (
	// Providers is our collection of what we provide to other services
	Providers = wire.NewSet(
		Provide{{ .Name }}sService,
		Provide{{ .Name }}DataManager,
		Provide{{ .Name }}DataServer,
	)
)

// Provide{{ .Name }}DataManager turns a database into an {{ .Name }}DataManager
func Provide{{ .Name }}DataManager(db database.Database) models.{{ .Name }}DataManager {
	return db
}

// Provide{{ .Name }}DataServer is an arbitrary function for dependency injection's sake
func Provide{{ .Name }}DataServer(s *Service) models.{{ .Name }}DataServer {
	return s
}
