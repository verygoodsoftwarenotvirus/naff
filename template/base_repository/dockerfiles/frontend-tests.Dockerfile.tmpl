FROM golang:stretch

WORKDIR /go/src/{{ .OutputRepository }}

RUN apt-get update -y && apt-get install -y make git gcc musl-dev

ADD . .

ENTRYPOINT [ "go", "test", "-v", "-failfast", "-parallel=1", "{{ .OutputRepository }}/tests/v1/frontend" ]
