---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Project.ProjectMeta.Kebab }}-admin-webapp-svc
  labels:
    app: {{ .Project.ProjectMeta.Kebab }}-admin-webapp
    app.kubernetes.io/name: {{ .Project.ProjectMeta.Kebab }}-admin-webapp
spec:
  selector:
    app.kubernetes.io/name: {{ .Project.ProjectMeta.Kebab }}-admin-webapp
  ports:
    - name: http
      port: 8888
      protocol: TCP
      targetPort: 3000
  type: ClusterIP
---
