import Foundation

/// {{ .Domain.Name | title }}Service provides API methods for the {{ .Domain.Name }} domain.
final class {{ .Domain.Name | title }}Service: Sendable {
    private let client: GRPCClient

    init(client: GRPCClient) {
        self.client = client
    }
{{ range .Domain.Entities }}
    // MARK: - {{ .Name }}

    /// Fetches {{ withArticle .Name }} by ID.
    func get{{ .Name }}(id: String) async throws -> {{ .Name }} {
        fatalError("TODO: implement gRPC call for get{{ .Name }}")
    }

    /// Lists {{ pluralHumanReadable .Name }}.
    func list{{ plural .Name }}() async throws -> [{{ .Name }}] {
        fatalError("TODO: implement gRPC call for list{{ plural .Name }}")
    }

    /// Creates {{ withArticle .Name }}.
    func create{{ .Name }}(input: {{ .Name }}CreationRequestInput) async throws -> {{ .Name }} {
        fatalError("TODO: implement gRPC call for create{{ .Name }}")
    }

    /// Updates {{ withArticle .Name }}.
    func update{{ .Name }}(id: String, input: {{ .Name }}UpdateRequestInput) async throws -> {{ .Name }} {
        fatalError("TODO: implement gRPC call for update{{ .Name }}")
    }

    /// Archives {{ withArticle .Name }}.
    func archive{{ .Name }}(id: String) async throws {
        fatalError("TODO: implement gRPC call for archive{{ .Name }}")
    }
{{ end -}}
}
