<!-- Generated by naff. Do not edit. -->
<script lang="ts">
	import { page } from '$app/stores';
</script>

<nav>
{{- range .Project.Domains }}
	<div class="nav-group">
		<h3 class="nav-group-title">{{ humanReadable .Name }}</h3>
		<ul>
{{- range .Entities }}
			<li>
				<a
					href="/{{ pluralKebab .Name }}"
					class:active={$page.url.pathname.startsWith('/{{ pluralKebab .Name }}')}
				>
					{{ pluralHumanReadable .Name }}
				</a>
			</li>
{{- end }}
		</ul>
	</div>
{{- end }}
</nav>

<style>
	nav {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.nav-group-title {
		font-size: 0.7rem;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		color: #888;
		margin: 0.5rem 0 0.25rem;
	}

	ul {
		list-style: none;
		padding: 0;
		margin: 0;
	}

	li a {
		display: block;
		padding: 0.4rem 0.75rem;
		color: #ccc;
		text-decoration: none;
		border-radius: 4px;
		font-size: 0.9rem;
	}

	li a:hover {
		background: rgba(255, 255, 255, 0.1);
		color: #fff;
	}

	li a.active {
		background: rgba(233, 69, 96, 0.2);
		color: #e94560;
	}
</style>
