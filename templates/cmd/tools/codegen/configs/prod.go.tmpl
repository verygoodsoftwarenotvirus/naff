package main

import (
	"{{ .Project.ProjectMeta.Module }}/internal/config"
)

// buildProdConfig returns the APIServiceConfig used in production (Kustomize).
// Populate fields inline and regenerate — NAFF will not overwrite the
// hand-authored interior once you replace this stub.
func buildProdConfig() *config.APIServiceConfig {
	return &config.APIServiceConfig{}
}
